model:
  name: vit-base # [resnet50, resnet50-gn, vit-base]

dataset:
  name: ImageNetC
  path: ./data/

  batch_size: 16
  num_workers: 16
  shuffle: True

optimizer:
  name: SGD
  lr: 0.00025
  weight_decay: 0.01
  momentum: 0.9

trainer:
  name: TestTrainer
